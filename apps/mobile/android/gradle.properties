org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# Proje D: sürücüsündeyse, Pub Cache (C:) ile farklı root olduğu için Kotlin incremental cache hata verebilir.
# Incremental kapatılırsa bu hata ortadan kalkar (ilk build biraz daha uzun sürebilir).
kotlin.incremental=false
